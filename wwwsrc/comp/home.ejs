<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Demo News - List of latest demos on Steam store</title>


    <!-- Hope some analytics won't harm privacy -->
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-W28YK6D037"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag() { dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', 'G-W28YK6D037');
    </script>

    <style>
        html,
        body {
            font-family: 'SF Pro Text', 'TeX Gyre Heros', 'Helvetica Neue', 'Segoe UI', 'Inter', 'Arial', sans-serif;
            font-size: 11px;
            color: white;
            background: none;
        }

        @media screen and (min-width: 940px) {

            html,
            body {
                font-size: 14px;
            }

            .limit-width {
                max-width: 600px;
                padding-left: 130px;
                padding-right: 130px;
                margin-left: auto;
                margin-right: auto;
            }
        }

        html {
            background: #111318;
        }

        .grand-container {
            padding: 1rem;
            max-width: 940px;
            margin: 0 auto;
        }

        a {
            color: inherit;
        }

        .header {
            text-align: center;
            margin: 0 0 30px;
        }

        .header p,
        .header a {
            font-size: 1.5rem;
        }


        h1 {
            font-size: 5rem;
        }

        .item-large {
            border-radius: 0.7rem;
            padding: 0 0;
            margin: 4rem 0;
            overflow: hidden;
        }

        .item-large .item--large-subinfo {
            background: rgba(0, 0, 0, 0.7);
            padding: 0 2.5rem 2.5rem;
        }

        .item-large img {
            display: block;
            width: 100%;
        }

        .item-large .item--title {
            font-size: 2.5rem;
            font-weight: 600;
            padding: 0.75em 0;
            margin: 0 0;
        }

        .item-large .item--date {
            font-size: 1.5rem;
            font-weight: 600;
            padding: 0 0 0 2rem;
            display: flex;
            flex-flow: column;
            justify-content: center;
        }

        .item-large .item--button {
            font-size: 1.75rem;
        }

        .item-small {
            max-width: 600px;
            padding: 0rem;
            margin: 2rem auto;
        }

        .item-small img {
            display: block;
            width: 100%;
            height: auto;
            margin: 0 1.5rem 0 0;
            border-radius: 0.7rem;
            overflow: hidden;
        }

        .item-small .item--right {
            display: none;
        }

        @media screen and (min-width: 940px) {
            .item-small {
                display: flex;
                padding-left: 130px;
                padding-right: 130px;
            }

            .item-small .img-anchor {
                display: block;
            }

            .item-small img {
                display: block;
                width: auto;
                height: 10.5rem;
            }

            .item-small .item--right {
                display: flex;
                height: auto;
                padding: 0.5rem 0;
                flex-flow: column;
                justify-content: space-between;
            }
        }

        .item-small .item--title {
            font-size: 1.7rem;
            font-weight: 600;
            padding: 0;
            margin: 0 0;
            flex-grow: 1;
        }

        .item-small .item--date {
            font-size: 1.0rem;
            padding: 0 0 0.75rem;
            margin: 0 0;
        }

        .item-small .item--button {
            font-size: 1.0rem;
        }

        .item--button {
            display: inline-block;
            font-weight: 600;
            color: black;
            text-decoration: none;
            background: white;
            border-radius: 0.5rem;
            padding: 0.5em 0.8em;
            transition: all 150ms ease;
        }

        .item--button:hover {
            transform: scale(1.1);
        }

        .item img {
            transition: all 150ms ease;
        }

        .item-large img:hover {
            transform: scale(1.015);
        }

        .item-small img:hover {
            transform: scale(1.03);
        }

        .item-small img:hover {
            box-shadow: rgba(100, 130, 255, 0.2) 0 4px 32px 1px;
        }

        .footer {
            max-width: 600px;
            margin: 10rem auto;
        }
    </style>
</head>


<body>
    <div class="grand-container">
        <div class="header">
            <div>
                <h1>DEMO NEWS</h1>
                <aside>
                    <p>List of latest demos on Steam store</p>
                </aside>
            </div>
            <nav>
                <div>
                    <a href="https://github.com/nekostein/demonews/blob/master/doc/REQUEST_FEATURED.md">Request Being Featured</a>
                </div>
            </nav>
        </div>
        <div class="content">
            <%- LISTCONTENT %>
        </div>
        <div class="footer limit-width">
            <p>Copyright &copy; 2024 <a href="https://nekostein.com/">Nekostein</a>. All rights reserved.</p>
            <p>This website is not affiliated with Valve.</p>
            <p>Marketing images of the games belong to their respective owners.</p>
        </div>
    </div>

    <div id="js-imgBackground" style="
        position: fixed;
        top: 0px;
        left: 0px;
        width: 100vw;
        height: 100vh;
        z-index: -100;
        background-color: #111318;
        background-size: cover;
        background-position: center;
        opacity: 0.03;
    "></div>
    <script>
        document.querySelectorAll('.item--date').forEach(node => node.innerHTML = (new Date(node.innerText * 1000).toISOString().slice(0, 10)));
        let itr = 0;
        const rotate_bg = function () {
            const nodes_arr = document.querySelectorAll('.item img');
            const url = nodes_arr[itr].src
            document.querySelector('#js-imgBackground').style.backgroundImage = `url("${url}")`;
            itr = (itr + 1) % nodes_arr.length;
        };
        rotate_bg();
        window.setInterval(rotate_bg, 5000);
    </script>
</body>

</html>
